{
  "name": "greenplus",
  "private": true,
  "workspaces": [
    "web-admin",
    "web-client"
  ],
  "scripts": {
    "dev:admin": "npm run dev --workspace web-admin",
    "dev:client": "npm run dev --workspace web-client",
    "build": "npm run build --workspace web-admin && npm run build --workspace web-client",
    "lint": "npm run lint --workspace web-admin && npm run lint --workspace web-client",
    "start:admin": "npm run start --workspace web-admin",
    "start:client": "npm run start --workspace web-client"
  },
  "devDependencies": {
    "dotenv": "^16.6.1"
  }
}
