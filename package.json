{
  "name": "greenplus",
  "private": true,
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "web-admin",
    "web-client",
    "web-shipper",
    "supabase-playground",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --ui=tui",
    "dev:admin": "turbo dev --filter=web-admin",
    "dev:client": "turbo dev --filter=web-client",
    "dev:shipper": "turbo dev --filter=web-shipper",
    "dev:playground": "turbo dev --filter=supabase-playground",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "turbo": "^2.8.19"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.102.1"
  }
}
